apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: funds-db
  namespace: coin-exchange
spec:
  serviceName: funds-db
  replicas: 1
  selector:
    matchLabels:
      app: funds-db
  template:
    metadata:
      labels:
        app: funds-db
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "1234"
            - name: MYSQL_DATABASE
              value: funds_db
          args:
            - --character-set-server=utf8mb4
            - --collation-server=utf8mb4_unicode_ci
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "localhost", "-uroot", "-p1234"]
            initialDelaySeconds: 20
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: funds-db
  namespace: coin-exchange
spec:
  selector:
    app: funds-db
  ports:
    - port: 3306
      targetPort: 3306
