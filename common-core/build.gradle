plugins {
    id 'java-library'
}

dependencies {
    api 'org.springframework.boot:spring-boot-starter-data-jpa'
    api 'org.springframework.boot:spring-boot-starter-web'
    // 분산 트레이싱 — 요청마다 traceId를 로그에 찍고 서비스 간 전파
    api 'io.micrometer:micrometer-tracing-bridge-brave'
}
